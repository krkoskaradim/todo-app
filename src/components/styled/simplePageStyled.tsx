import styled from 'styled-components';
import { Layout } from 'antd';

export const SimplePageStyled = styled(Layout)`
    min-height: 100vh;
    position: relative;
    display: flex;
    padding: 2rem;
`;
